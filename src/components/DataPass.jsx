import React from 'react';

const DataPass = (props) => {
    return (
        <div>
            <h1> {props.title} </h1>
            <h1> {props.des} </h1>
        </div>
    );
};

export default DataPass;